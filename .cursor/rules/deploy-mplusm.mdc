---
description: 腾讯云生产部署固定流程（3d.mplusm.site）；用户提到上线、发版、服务器时使用
alwaysApply: false
---

# 生产环境约定

- **域名**：`3d.mplusm.site` → **IP**：`43.133.145.77`（若用户未说明变更，按此文档）
- **服务器应用目录**：`/var/www/3d-mplusm`（从 `https://github.com/GongDeejay/3D.git` 克隆）
- **PM2 进程名**：`3d-mplusm`（见仓库根 `ecosystem.config.cjs`）
- **发布脚本**：仓库内 `deploy/release.sh`；完整步骤见 **`deploy/MPLUSM.md`**
- **环境变量**：`NEXT_PUBLIC_BASE_URL=https://3d.mplusm.site`；密钥仅在服务器 `.env`，永不写入 Git

协助部署时：优先让用户按 `deploy/MPLUSM.md` 操作；不要建议在仓库或聊天中保存 root 密码、SecretKey。
